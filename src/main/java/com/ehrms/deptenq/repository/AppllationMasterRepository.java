package com.ehrms.deptenq.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.ehrms.deptenq.models.AppellationMaster;

public interface AppllationMasterRepository extends JpaRepository<AppellationMaster, Long>{

}
